import { Link } from 'react-router-dom';

const Layout = () => {
    return (
      <main style={{ padding: '1rem 0' }}>
        <Link to="/day-00">Day 00: Setup</Link>
      </main>
    );
  };

export default Layout;
