import { Link, Outlet } from 'react-router-dom';

const Layout = () => {
    return (
      <main style={{ padding: '1rem 0' }}>
        <Link to="/day-00">Day 00</Link>
      </main>
    );
  };

export default Layout;
